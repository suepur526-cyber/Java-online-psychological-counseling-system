server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:13307/counseling_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: counseling
    password: counseling123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.example.counseling.model
  configuration:
    map-underscore-to-camel-case: true

app:
  cors:
    allowed-origins: http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:15200
